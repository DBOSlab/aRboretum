% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arboretum_audios.R
\name{arboretum_audios}
\alias{arboretum_audios}
\title{Create folder structure and phrase files for personal audio recordings}
\usage{
arboretum_audios(
  data_path = NULL,
  printed_lang = c("pt", "en", "fr", "es"),
  add_lang = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data_path}{Character string. Path to the input data file (CSV or Excel format)
generated by \code{arboretum_data()}.}

\item{printed_lang}{Character vector. Built-in language(s) to generate folders and
phrases for. Accepted values are \code{"pt"}, \code{"en"}, \code{"fr"}, and \code{"es"}.
Default is \code{c("pt", "en", "fr", "es")}.}

\item{add_lang}{Character string or \code{NULL}. Optional code for one additional
language to include in the folder structure for personal recordings, for
example \code{"PANARA"} or \code{"TUKANO"}. This argument is intended for cases where
users want to add custom community or local-language audio without translating
the full package interface. When supplied, one extra recording folder per
species is created using that language code.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages to the console.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
This function creates an organized folder structure for storing personal audio
recordings of species descriptions and generates an HTML guide with the phrases
to be recorded. It is designed to be used \strong{after} running \code{arboretum_data()}
and \strong{before} running \code{arboretum_labels()} when users want to provide their
own audio files instead of relying only on browser text-to-speech.

In addition to the built-in translated languages (\code{"pt"}, \code{"en"}, \code{"fr"},
and \code{"es"}), the function can optionally prepare folders for one additional
language through \code{add_lang}. This is useful when a project needs to include
community-based or locally relevant audio, such as recordings in an Indigenous
language, even if that language is not yet fully incorporated into the package
interface or translation system.

The \code{add_lang} argument is intended as a flexible, intermediate solution:
it allows users to organize and add personal audio for an extra language while
the rest of the website remains in the package-supported interface languages.
}
\details{
The function creates an \verb{arboretum_audios/} directory containing one folder per
species-language combination. Folder names follow the pattern:

\preformatted{
FAMILY_Genus_species_LANG
}

where \code{LANG} is one of \code{PT}, \code{EN}, \code{FR}, \code{ES}, or the custom value supplied in
\code{add_lang}.

It also creates an HTML file named:

\preformatted{
arboretum_audios/__personal_audio_recording_guide.html
}

This guide helps users navigate across species and languages when preparing
personal recordings.
}
\note{
\code{add_lang} does not create a full translation workflow for the package interface.
It only prepares an additional language slot for custom audio recording, and is
especially useful in collaborative projects where accessibility in a community
language is needed before a complete website translation is available.
}
\author{
Martin Boucknooghe & Domingos Cardoso
}
